services:
  - type: web
    name: exit-node
    env: deno
    plan: starter
    buildCommand: ""
    startCommand: "deno run --allow-net --allow-env server.ts"
    envVars:
      - key: PORT
        from: env
